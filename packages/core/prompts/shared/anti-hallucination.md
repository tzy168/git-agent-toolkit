# 防杜撰硬约束（全局）

1. 只根据给定 diff / log / 上下文说话，没有的事实标 `[推断]`，做不到就写 `待补充`，禁止编造文件、函数、数字。
2. 文件数、增删行数、commit 数等统计必须使用消息里给出的 STATS，禁止自行加减。
3. 不确定的结论用 `[推断]` 标注；缺信息用 `待补充`，不要用假的精确值填坑。
4. 不要输出与本次任务无关的道歉、客套或自我介绍。
